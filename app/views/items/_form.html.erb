<%= simple_form_for item do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.select :category, [  "baby", "sport & outdoors", "electronics", "books", "videogrames", "toys", "luggage", "men’s fashion", "women’s fashion", "pet supplies" ] %>
  <%= f.input :location %>
  <label class="btn btn-primary circular-button">
    Add a photo
    <span style="display:none;">
    <%= f.input :photo, as: :file %>
    </span>
  </label>
  <label class="btn btn-primary circular-button">
  Create Item
  <span style="display:none;">
    <%= f.submit %>
  </span>
</label>
<% end %>
<%# ----below how to add a class to a field %>
<%# <%= f.input :photo, input_html: { class: 'photo-field' }, as: :file %>

<%# -----below the original simple_form fields with default buttons %>
<%# <%= f.input :photo, as: :file %>
  <%# <%= f.submit %>